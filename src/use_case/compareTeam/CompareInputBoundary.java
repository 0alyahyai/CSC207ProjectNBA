package use_case.compareTeam;

import use_case.login.LoginInputData;

public interface CompareInputBoundary {
    void execute(); //this will be empty, because to show the data of all the teams (the names) there is no input.
}
